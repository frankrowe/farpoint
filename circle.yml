machine:
  xcode:
    version: 9.0.0
dependencies:
  pre:
    - npm -g install react-native-cli
    - npm i
    - brew install android-sdk

test:
  override:
    - cd android && ./gradlew clean build assemble
